/* The reason we created this file is because we want to refer all action type strings to be consistent and avoid typo. */

export const userActionTypes = {
  SET_CURRENT_USER: 'SET_CURRENT_USER',
};
